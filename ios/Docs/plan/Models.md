# Plan — `Models/` (Batch 0, one owner; Foundation only)

Interfaces: `INTERFACES.md` → Models/. Every type there is frozen. Files marked (keep) exist and
compile; reshape them to the interface, do not start from zero. `MediaStudioModels.swift` is deleted
and replaced by `MediaModels.swift`.

| File | Purpose | Behaviour / decoding rules | Read |
|---|---|---|---|
| `Models/CommonModels.swift` (keep) | `AppLanguage`, `ModelTier`, `ProductKind`, `ResponseMode`, `FeatureKey`, `AppAPIValue` | Move `AppLanguage` here from `FirasTheme.swift`; `deviceDefault` = first preferred language starts with `en` → `.english` else `.arabic`. Drop the two `@MainActor` funcs on `ModelTier`; labels/taglines/short/badge as `LText`; `tokenCap` 2048/16384/16384/16384. `ProductKind.studio` + `wireValue` (`studio → "ai"`); titles `فِراس AI / فِراس Agent / فِراس Code / فِراس Brain / الاستوديو`. | `web-chat-ux.md §3.1`, `design-brief.md §0 (1)`, `audit-ios-networking-auth.md §B7 F21` |
| `Models/AuthModels.swift` (keep, prune) | `User`, `SubInfo`, `QuotaCounts`, `PlanKind`, `PendingSignup`, verification DTOs | Delete Firebase/OAuth-exchange DTOs. `User.guest` defaults false (members never carry the key); `SubInfo.expiresAt` ms `Double?`; `-1` = unmetered; `PlanKind` unknown → `.free`; `plan == "guest"` for guests. | `server-auth-session-account.md §3.2–3.4, §4.1–4.3` |
| `Models/ChatModels.swift` (keep) | conversation + message shapes | `ChatMessage.init(from:)` lenient; `id` = `cid` when present else UUID; `status = .delivered` for decoded rows; `ChatSummary.product` derives from flags (`agent` → `.agent`, `codeProj` → `.code`, `brainNb` → `.brain`, else `.ai`). `CreateChatRequest.id` only for guest migration. | `server-chat-jobs-chats.md §5.1–5.3`, `audit-ios-chat.md §D` |
| `Models/ChatWireModels.swift` | wire DTOs | `PersistedMessage` fields = the whitelist and nothing else; `ChatJobStatus.init(from:)` tolerates `{"phase":"unknown"}` and `error` as JSON string; `LongFileProgress` all-optional-with-defaults; `ShareInfo.url` = `https://firasai.org/?share=<id>`. | `server-chat-jobs-chats.md §1.2, §3.1–3.2, §4.2, §4.4, §5.2, §6` |
| `Models/JobModels.swift` | pointer/snapshot/terminal | `JobPhase.init(raw:)` maps `done→completed`, `fail→failed`, `run/exec/running→processing`, else `.unknown`. `JobKind.product`: chat/longdoc/longfile → `.ai`, agentrun → `.agent`, codebuild → `.code`, brainask → `.brain`, media → `.studio`. `JobPointer` Codable with `Date` as `timeIntervalSince1970` (explicit `init(from:)`). | `ARCHITECTURE.md §2.4` |
| `Models/AgentModels.swift` (keep) | agent surface | Add `artifactIndex` (parse `index=` from `url`), `AgentBusyResponse`, `AgentJob.parseFence` (```` ```firas-agent ```` body → JSON; brace-match fallback for truncated fences). `AgentJobPhase` synonyms kept. | `server-agent.md §6, §7`, `web-agent-ux.md §6.2–6.3`, `audit-ios-agent-code.md §F` |
| `Models/CodeModels.swift` (keep) | project fence, caps, thread, edit plan | `blankFiles` = the web `CW_BLANK_FILES` scaffold; `validatedForSave` caps 30 / 120 / 60 000 / 180 000; `CodeChatThread` = base64(JSON) in ```` ```firas-code-chat ````; `CodeEditPlan.parse` handles ```` ```file:path ```` blocks, `DELETE: path`, `RENAME: a -> b`, placeholder guard (reject bodies that are only "…"/"same as before"). | `web-code-ux.md §0, §1.1–1.3, §6.3`, `server-code-brainask.md §2.4–2.5` |
| `Models/BrainModels.swift` (keep) | brain DTOs | Add `near`, lenient `kind`/`unit` (unknown → `.text` / `.page`), `BrainWholeRequest/Response`, `BrainSource` (the persisted source pointer), `BrainAskJobRequest`. | `server-brain.md §4–§10, §13, §16`, `web-brain-ux.md §3` |
| `Models/MediaModels.swift` | media DTOs + fence meta + creation record | `ImageShape` pixels 1024²/1024×1536/1536×1024; `VideoJobRequest.image` = JPEG base64 first frame; `MediaMeta.parse/encodedFence` for `firas-image`/`firas-video`/`firas-music` exactly as the web writes them; `MediaCreation.localFilename` relative (never a URL). | `server-media.md §1.7, §2.6, §3.5`, `web-media-ux.md §9`, `audit-ios-brain-media.md §B.3` |
| `Models/VoiceModels.swift` | `LiveToken`, `CallVoice`, `DictationDialect`, `TranscribeResponse` | 14 dialect keys, labels and flags from `MIC_LANGS`; `bcp47` for `SFSpeechRecognizer` (`auto → ar-SA`). | `server-voice.md §3.3–3.4, §4.1`, `web-voice-call-mic.md §7.2` |
| `Models/FenceModels.swift` | `FirasFence`, `CodeMeta`, `FileMeta` | `firstFence` returns the first ```` ```firas-* ```` (or `plot`) fence; `parse` per name; `firas-code` = JSON meta line then code; `firas-file` = JSON; `firas-project` → `CodeProject.decode`; `firas-ask` → `AskSpec.parse`; `firas-sources` → `[BrainSource]`; unknown → nil (render as code). | `web-chat-ux.md §8.5–8.7`, `server-code-brainask.md §4.6`, `server-chat-jobs-chats.md §4.3`, `web-brain-ux.md §11.2` |
| `Models/AccountModels.swift` | `Announcement` (+`builtinLaunch` verbatim from `brand/announcement-launch.json`), `MemoryEntry` | Announcement `at` ms; `pinned` default false. | `server-misc.md §9.2–9.3`, `web-auth-account-settings.md §8` |
| `Models/SettingsModels.swift` (keep) | backup format, change-* DTOs | Unchanged except naming aligned to `PersistedMessage`. | `audit-ios-shell-settings-design.md §1 Models` |

Rules: no `Date` decoding strategies; every server-optional field `Optional`; enums with server
strings get `init(from:)` with a fallback; nothing imports SwiftUI.
