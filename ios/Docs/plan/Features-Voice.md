# Plan — `Features/Voice/` (Batch 1; 3 owners: Engine+AudioGraph+Arbiter+ThreeHop · Transports+EchoGuard · Screen+Orb+Dictation+TTS; also `Localization/Strings+Voice.swift`)

Interfaces: `INTERFACES.md` → Features/Voice/. Design: `ARCHITECTURE.md §2.13–2.14`,
`audit-ios-voice.md §D` (adopted verbatim), `design-brief.md §7.13–7.14`. The Codex
`LiveVoiceController.swift`/`VoiceCallView.swift` are deleted; salvage the Gemini JSON protocol,
`finish()` re-entrancy, resampler idea and `pcmLevel` from the old controller.

| File | Purpose | Behaviour | Read |
|---|---|---|---|
| `Features/Voice/AudioSessionArbiter.swift` | the only `AVAudioSession` caller | Priority `call > record > playback > uiSound`; `acquire` configures category/mode/options per owner off-main (`Task.detached`), refuses lower-priority owners while a higher one holds; `release(.notifyOthersOnDeactivation)`. | `audit-ios-voice.md §D4, §B`, `design-brief.md §5.2` |
| `Features/Voice/CallAudioGraph.swift` | engine graph | §2.13 recipe: VPIO before formats, converter to Int16 mono target rate, 100 ms tap → `AsyncStream<Data>(bufferingNewest: 8)`, Float32 24 kHz player node, serial queue, `isEnding` latch, every `scheduleBuffer/play` guarded by `engine.isRunning`; observers for configuration change / route change / interruption; `flushPlayback` returns played ms for truncate. | `audit-ios-voice.md §B, §D4`, `server-voice.md §7.7` |
| `Features/Voice/CallTransport.swift` | protocol + events | As in INTERFACES. | `audit-ios-voice.md §D2–D3` |
| `Features/Voice/OpenAIRealtimeTransport.swift` | WS GA vocabulary | `wss://api.openai.com/v1/realtime?model=` with `Authorization: Bearer ek_…`; assert `session.created` (instructions start "You are Firas", `semantic_vad`) → `.ready`; `input_audio_buffer.append` base64 PCM16 24 kHz; `response.create` greeting; `response.audio.delta` → `.audio`, transcripts → captions, `speech_started/stopped`, `response.created/done`, `conversation.item.truncate`; ping every 20 s while gated; `URLSessionWebSocketTask` with its own session invalidated on close; 12 s connect deadline. | `server-voice.md §3.7, §8`, `web-voice-call-mic.md §4.1` |
| `Features/Voice/GeminiLiveTransport.swift` | Gemini Live | Setup frame with the verbatim instruction, voice, `tools:[googleSearch]` unless `reducedSetup`/no-search list; `setupComplete` within 10 s; 16 kHz PCM in, 24 kHz out; `interrupted` → flush; close codes 1008/1011 surfaced with reason; token single-use (fresh mint per attempt). | `server-voice.md §3.8, §7.9`, `web-voice-call-mic.md §5` |
| `Features/Voice/EchoGuard.swift` | Gemini echo | RMS floor / ×3 playback ratio / 3 frames / 350 ms hangover → substitute silence. | `server-voice.md §7.4` |
| `Features/Voice/ThreeHopCall.swift` | fallback rung | record until silence → `transcribe` → `chatStream` `{tier ≤ pro, think:false, nomem:false}` with `PromptCatalog.callSys` → `tts`; captions from transcript/reply. | `web-voice-call-mic.md §6`, `server-voice.md §7.1` |
| `Features/Voice/CallEngine.swift` | state machine | §2.13 in full: ladder, deadlines on every await, two clocks, mic gate, barge-in setting, guest cap sentence, diagnostics toast for members (`Live: <engine> — <reason>`), background continuation + `notifications.postCallEnded`, `jobs.callActive`/`tts.callActive` flags, `onPause/onResume` for plan cycles, idempotent `end`. Retry = full teardown then `start`. | `audit-ios-voice.md §D1–D6`, `server-voice.md §3.2, §3.5, §6, §7`, `web-voice-call-mic.md §2.2` |
| `Features/Voice/CallScreen.swift` | `fullScreenCover` | `callGround` + radial, name + timer (LTR), `OrbView` 176 pt, status/caption line (`callConnecting/Listening/Thinking/Speaking/Muted`), consent card before permission (`callConsentText/Btn`), controls mute/end/speaker in `GlassEffectContainer(spacing: 20)` on 26 (`#available` here only), Retry on `failed`, guest cap copy, iPad scaling, `esc`/space shortcuts; `Haptics.callConnected/callEnded`. | `design-brief.md §7.13`, `server-voice.md §6`, `web-voice-call-mic.md §2.1–2.3` |
| `Features/Voice/OrbView.swift` | orb | `TimelineView(.animation)` + `Canvas`; level eased rise 0.45 / fall 0.10; rotation `0.30 + level × 2.4` rad/s; no halo; brightness-only under reduced motion; no glass inside. | `design-brief.md §3.1`, `web-voice-call-mic.md §2.3` |
| `Features/Voice/DictationRecorder.swift` | recorder | Engine tap → converter → 16 kHz Int16 mono; 300 s cap; level stream ≤ 20 Hz; `cancel` discards. | `web-voice-call-mic.md §7.3`, `server-voice.md §4.4` |
| `Features/Voice/WAVEncoder.swift` | RIFF | 44-byte header, PCM16, base64 standard alphabet, no line breaks. | `server-voice.md §4.1` |
| `Features/Voice/DictationController.swift` | flow | `start` (permission → `micDenied` toast) → `finish`: < 700 ms or < 1 500 bytes → `micTooShort`; `transcribe` (503/offline → `SFSpeechRecognizer` on-device; needs `NSSpeechRecognitionUsageDescription`); empty → `micEmpty`; other errors → `micFail`; returns text; `applicationDidEnterBackground` auto-finishes; `Haptics.recordStart/Stop`. | `server-voice.md §4, §6`, `web-voice-call-mic.md §7` |
| `Features/Voice/DialectPickerSheet.swift` | 14 dialects | Radio rows with flag + label from `DictationDialect`; writes `prefs.dictationDialect`. | `web-voice-call-mic.md §7.2` |
| `Features/Voice/TTSPlayer.swift` | Listen | §2.14: `speakable` cleaning, chunking, cache, 429 → `AVSpeechSynthesizer` from the failed chunk with `listenLocal`, no Arabic voice → `listenNoVoiceAr`, `callActive` → `listenBusy`; one speaker token; `AudioSessionArbiter.acquire(.playback)`. | `server-voice.md §5, §6, §7.8`, `web-voice-call-mic.md §8` |

Strings: `Strings.Voice` from `server-voice.md §6` and `web-voice-call-mic.md §7.2, §8`.
Rules: never `setActive` outside the arbiter; never `play()` outside the graph; every await in the
engine is bounded; the call must survive `didEnterBackground` (`UIBackgroundModes: audio`).
