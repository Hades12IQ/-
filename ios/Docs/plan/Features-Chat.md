# Plan — `Features/Chat/` (Batch 1; 3 owners: Screen+Transcript+Messages · Composer · Sheets/Viewers/Controllers)

Interfaces: `INTERFACES.md` → view entry points, `ComposerView`. Design: `design-brief.md §7.1–7.7`.
Delete `ChatMessageRow.swift`, `ChatComposer.swift`, `ModelSelectionSheet.swift`,
`LiveVoiceController.swift`, `VoiceCallView.swift`, `ChatStrings.swift`, `Chat.xcstrings`. Keep
`ChatScreen.swift` (frame), `AddContextSheet.swift`, `ChatAttachmentProcessor.swift` (names unchanged).

| File | Purpose | Behaviour | Read |
|---|---|---|---|
| `Features/Chat/ChatScreen.swift` (keep frame) | the conversation screen (ai/agent/brain share it) | `NavigationStack` toolbar: drawer/sidebar button (compact), principal `TierPill`, trailing new-chat + gallery; `safeAreaInset(edge: .bottom)` composer (`safeAreaBar` on 26 inside `#available`); `TranscriptView` or `WelcomeView`; banners: offline strip, `sessionExpiredNotice`, `state.errorStrip`, history-trimmed note; drop (`.dropDestination(for: Data.self)`) and paste → attachments; sheets are routed via `router`. | `design-brief.md §7.1`, `audit-ios-chat.md §D`, `web-chat-ux.md §2` |
| `Features/Chat/TranscriptView.swift` | scroll + rows | `ScrollView` + `LazyVStack` of `AssistantTurnView`/`UserTurnView` (each `Equatable`); `onScrollGeometryChange` tracks `isAtBottom` (≤ 48 pt); autoscroll only when pinned to bottom (no animation under reduced motion); floating scroll-to-bottom chip (`.floating`); `SkeletonView(.transcript)` while loading; reading column 760/980. | `audit-ios-chat.md §Critical C5`, `design-brief.md §7.1, §3.3` |
| `Features/Chat/WelcomeView.swift` | empty conversation | Halo + mark + greeting by time of day + first name; Agent variant with templates strip; entrance per brief. | `web-chat-ux.md §12`, `design-brief.md §3.1, §7.1` |
| `Features/Chat/TierPill.swift` | principal pill | Short tier name + symbol; Max purple `maxTier*`; tap → `router.sheet = .tierPicker`; `FirasMotion.tierPop` on change; `differentiateWithoutColor` badge text. | `web-chat-ux.md §3`, `design-brief.md §7.4, §3.3` |
| `Features/Chat/TierPickerSheet.swift` | rewrite of ModelSelectionSheet | `.sheet` glass (no solid presentationBackground on 26), medium detent (popover on iPad), four rows (label, tagline, badge `الأقوى`/`للأكواد`), Think toggle hidden on mini, response-style card (`تلقائي/تخطيط`), content entrance `.snappy(duration: 0.42, extraBounce: 0.035)`; no sheet-level RTL. | `web-chat-ux.md §3.1–3.3, §4, §6`, `design-brief.md §7.4, §9` |
| `Features/Chat/ModePill.swift` | mode menu | `تلقائي/تخطيط` `Menu` with hints; writes `prefs.responseMode`; mid-cycle switch shows the one-line note. | `web-plan-mode.md §1.1, §7.2`, `web-chat-ux.md §6` |
| `Features/Chat/ComposerView.swift` | the composer | Two rows, `.floating` glass radius 24, `GlassEffectContainer(spacing: 12)` on 26 (`#available` in this file only); row 1 `ComposerField` + `AttachmentTray`; row 2 `+` (add sheet, badge), `ModePill`, search/think toggles (think hidden on mini), `LengthMeter`, mic (→ `DictationBar` replaces row 2), call button (→ `router.cover = .call`), send/stop morph (`FirasMotion.composer`); disabled states with toasts (never silent); disclaimer line under the composer; `Haptics.send()` on the frame the bubble appears; Agent product: "cannot be stopped" instead of Stop; drafts via `DraftStore`. | `design-brief.md §7.3`, `web-chat-ux.md §7`, `audit-ios-chat.md §Major M4–M7, M19` |
| `Features/Chat/ComposerField.swift` | text field | `TextField(axis: .vertical)` 1–6 lines; direction re-evaluated while typing (`bidiIsland`); Return = newline unless `prefs.sendOnReturn`; `⌘↩` always sends; `.onKeyPress` for `/` → `SlashMenu`; `.submitLabel(.send)` only when send-on-return. | `web-chat-ux.md §7.1`, `design-brief.md §7.3` |
| `Features/Chat/AttachmentTray.swift` | chips/thumbs | 64 pt thumbs, file chips with kind tag, `reading…`/`truncated` states, remove buttons, limits per `AttachmentProcessor`. | `web-chat-ux.md §7.3` |
| `Features/Chat/LengthMeter.swift` | chars·tokens | Thresholds near/over by `tier.tokenCap`; 200 000 hard cut with toast. | `web-chat-ux.md §7.1` |
| `Features/Chat/SlashMenu.swift` | quick commands | Four commands; inserting replaces the typed `/…` token with the verbatim body. | `web-chat-ux.md Appendix A — Slash-command bodies` |
| `Features/Chat/AddContextSheet.swift` (keep, fix) | `+` sheet | Camera / Photos / Files rows, tools toggles (search, thinking hidden on mini, dialect row → `router.sheet = .dialectPicker`), Brain OCR row; remove RTL forcing and opaque background; async ImageIO thumbnails. | `audit-ios-chat.md §D (AddContextSheet)`, `design-brief.md §7.3.2` |
| `Features/Chat/ChatAttachmentProcessor.swift` (keep) | files → `PreparedAttachment` | ImageIO downsample (≤ 2048 px, JPEG 0.82, raw base64 without prefix, thumb ≤ 300 000 chars data URL), text/PDF/Office via `OfficeDocumentExtractor`, budgets aware of history size; off-main. | `audit-ios-chat.md §Major M15–M16, §Critical C3` |
| `Features/Chat/DictationBar.swift` | recording row | Cancel, 32-bar waveform from `dictation.level`, timer (LTR), dialect chip, done ✓; states `micListening`/`micTranscribing`; result appended to the draft. | `design-brief.md §7.14`, `web-voice-call-mic.md §7` |
| `Features/Chat/UserTurnView.swift` | user bubble | Trailing, `userFill` + `userSheen` top hairline + `userEdge`, radius 20 (bottom-trailing 7), 12-line clamp with expand, image grid (thumbs), file chips, long-press copy/edit; bidi island. | `web-chat-ux.md §8.1`, `design-brief.md §7.6` |
| `Features/Chat/AssistantTurnView.swift` | assistant turn | Header (mark, `FIRAS`, tier badge), retry strip (`retryOf`), `ThinkingDisclosure`, `MarkdownView` with `onFence` → cards, streaming caret, `AskPanelView`/`PlanStartPill` when applicable, `QuickReplies` (hidden with pill/ask), `MessageActionsRow`, `VersionPager`; per-row `Equatable`; no glass; reveal animation `FirasMotion.reveal` after completion. | `web-chat-ux.md §8.2–8.4`, `design-brief.md §7.6–7.7`, `web-plan-mode.md §7.7` |
| `Features/Chat/ThinkingDisclosure.swift` | reasoning | `التفكير` chevron; live "يفكّر…" header while `liveReasoning` streams; collapsed by default after completion. | `web-chat-ux.md §4, §8.4` |
| `Features/Chat/MessageActionsRow.swift` | actions | Copy, regenerate (menu: same tier / retry with Max), listen (`tts.toggle`), share (one answer), `المزيد` (export md/text/PDF, continue, translate for members); hover-reveal 55 → 100 % on iPad. | `web-chat-ux.md §9–§10`, `design-brief.md §7.7` |
| `Features/Chat/MessageContextMenu.swift` | long-press | The web's order (§7.7 brief); system haptic only. | `design-brief.md §7.7` |
| `Features/Chat/VersionPager.swift` | `‹ 2/3 ›` | Over `alts`; `chat.selectVersion`; Arabic-Indic digits. | `web-chat-ux.md §10` |
| `Features/Chat/PlanStartPill.swift` | `ابدأ التنفيذ` | Accent-filled capsule with play icon above the actions row; `chat.approvePlan`. | `web-plan-mode.md §3.7, §7.7` |
| `Features/Chat/AskPanelView.swift` | `firas-ask` wizard | Stepper, radio/checkbox, recommended pre-selected with badge, extra field on the last step, `تأكيد الاختيارات` (disabled when nothing selected and extra empty), `تم الإرسال` when answered, disabled-with-toast while streaming; `askPreparing` loader while the fence is open; malformed → raw markdown. | `web-plan-mode.md §3.3–3.6, §7.7` |
| `Features/Chat/CodeViewerSheet.swift` | artifact viewer | Full-screen `معاينة/الكود` segmented; sandboxed non-persistent `WKWebView` (no navigation, `firas-preview://` base); code side = `CodeBlockView`. | `web-chat-ux.md §8.6`, `server-code-brainask.md §2.9` |
| `Features/Chat/LongFileViewer.swift` | long file | Fetch manifest + parts (`sha256` verified), strip `FIRAS_QUESTION_*` markers, page reader with `MarkdownView`, export via `ExportController`. | `server-chat-jobs-chats.md §4.2–4.4` |
| `Features/Chat/SharedChatView.swift` | `?share=` target | Read-only transcript from `GET /api/share?id=` + CTA; deep-link route. | `web-auth-account-settings.md §9.4` |
| `Features/Chat/ShareController.swift` | share | Members: `createShare` → `ShareInfo.url` → `UIActivityViewController` (via `ShareLink`); guests → `.signUpPrompt(.share)`; 429 → 20-share cap copy. | `server-chat-jobs-chats.md §6`, `web-auth-account-settings.md §9` |
| `Features/Chat/ExportController.swift` | export | Markdown / plain text / PDF (`ImageRenderer` of `MarkdownView`) / image → temp file → share sheet; flatten math to Unicode. | `web-chat-ux.md Appendix A — export` |

Rules: no row re-creates the whole conversation on a tick; only the streaming row observes
`ConversationState.liveText`; every fence goes through `FirasFence.parse` once per message id.
