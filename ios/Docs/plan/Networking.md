# Plan — `Networking/` (Batch 0)

Interfaces: `INTERFACES.md` → Networking/. Delete `Networking/FirasAPI.swift` (endpoints become
`extension APIClient`). Keep `GoogleOAuthProvider.swift`. Rewrite `APIClient.swift` around the
existing actor (cookie handling and safe filename parsing survive).

| File | Purpose | Behaviour | Read |
|---|---|---|---|
| `Networking/RequestBudget.swift` | budget → timeout + session choice | `.interactive` 12 s, `.poll` 30 s, `.upload` 300 s, `.download` 60 s, `.stream` 300 s (idle, re-armed by consumer). Three `URLSession`s: default (interactive/poll), upload, download; all `waitsForConnectivity = false`, `httpCookieStorage = .shared`, `httpCookieAcceptPolicy = .always`, `httpShouldSetCookies = true`. | `audit-ios-networking-auth.md §B3 F11–F13`, `server-auth-session-account.md §0.2` |
| `Networking/APIError.swift` | `APIError`, `ServerError`, `QuotaInfo`, `AgentActiveJob` | `ServerError.parse(_ data:)`: try JSON object → lenient fields; else `code = trimmed text` (`auth required`, `not found`, `rate limited`). `parse(jsonString:)` for job `error` strings. Never throws. | `server-auth-session-account.md §0.4`, `server-misc.md §13.4`, `server-agent.md §4.2`, `server-media.md §0.3` |
| `Networking/LenientJSON.swift` | `LenientJSON`, `AnyCodingKey` | Helpers accept number-or-string for ints, `"true"/1` for bools; used by every hand-written `init(from:)`. | `audit-ios-networking-auth.md §B5` |
| `Networking/SSEParser.swift` | bytes → `SSEFrame` | Buffer partial lines; handle CRLF; `: keepalive` comments ignored; multi-line `data:` joined with `\n`; `[DONE]` emitted as a frame with `data == "[DONE]"`; `retry:` ignored. | `server-chat-jobs-chats.md §1.7`, `server-agent.md §8` |
| `Networking/APIClient.swift` (rewrite) | the actor | `json/raw/stream/download`; GET → `.reloadIgnoringLocalCacheData` + `Cache-Control: no-cache`; query via `percentEncodedQueryItems` escaping `+&=`; JSON body `Content-Type: application/json; charset=utf-8`; non-2xx → `.http(status, ServerError.parse(data), raw)`; 401 → `unauthorized` continuation yield **after** throwing path is prepared; `URLError.cancelled` → `.cancelled`, `.notConnectedToInternet/.timedOut/.cannotFindHost/.networkConnectionLost` → `.offline` when `NetworkMonitor` says offline else `.transport`; decoding failure → `.decoding(String(describing: error))` logged in DEBUG. `download` keeps `Content-Disposition` filename parsing (safe filename). | `audit-ios-networking-auth.md §B3, §C`, `server-auth-session-account.md §0.1–0.5` |
| `Networking/GoogleOAuthProvider.swift` (keep) | PKCE via `ASWebAuthenticationSession` | Apply audit F17: `nonisolated presentationAnchor` + `callback: .customScheme`; expose `authorize(anchor:) async throws -> (idToken, accessToken?, nonce?)`. | `audit-ios-networking-auth.md §B4`, `server-auth-session-account.md §4.10` |
| `Networking/Endpoints/AuthEndpoints.swift` | auth + guest + account | Paths and bodies exactly per §4.1–4.17; `signup` returns `PendingSignup` (pid+email) or a `user` when the server signs in directly; `me` = `GET /api/auth/me` → `{user}`; `googleNative` body `{idToken, accessToken?, nonce?}`; `redeem` → `{user}`. | `server-auth-session-account.md §4, §5.1` |
| `Networking/Endpoints/ChatEndpoints.swift` | chats, stream, search, fetch, translate, share | `/api/chats` list/get/create/update/delete; `chatStream` = `POST /api/chat` SSE; `webSearch` `GET /api/search?q=&n=` (8 s explicit budget applied by caller); `fetchURL` `GET /api/fetch?u=`; `translate` `POST /api/translate`; share create/get/delete. | `server-chat-jobs-chats.md §1, §5.3, §6`, `server-misc.md §1, §3, §5` |
| `Networking/Endpoints/JobEndpoints.swift` | durable queue | `startChatJob` `POST /api/chat/job` (`.interactive`), `chatJobStatus` `GET /api/chat/job?id=` (`.poll`), `cancelChatJob` `POST /api/chat/cancel {id}` → true on `stopped`, 409 → false (not an error), `longFileManifest/Part` `GET /api/chat/job/file?id=[&part=]`, `usageCharge` `POST /api/usage/charge {product, units}`. | `server-chat-jobs-chats.md §3, §4.4`, `server-auth-session-account.md §5.2` |
| `Networking/Endpoints/AgentEndpoints.swift` | agent | `agentJob` decodes `{job: AgentJob?}`; `agentJobStream` SSE GET; `agentCredits`; `agentArtifact` download with `download=1` when requested. | `server-agent.md §7, §8, §10.2, §12.4` |
| `Networking/Endpoints/BrainEndpoints.swift` | brain | docs/doc(add, delete)/search/passage(`?doc=&i=&w=`)/whole; upload uses `.upload` budget. | `server-brain.md §5–§10` |
| `Networking/Endpoints/MediaEndpoints.swift` | media | start/status per kind (`/api/image/job`, `/api/video/job`, `/api/music/job`); `downloadMedia`: image `GET /api/image?key=`, video `GET /api/video/file?id=`, music `GET /api/music/file?id=`; `imageQuota` POST; `videoQuota` GET; `editImage` POST. Never send `image` to `/api/image/job` (501). | `server-media.md §1–§3` |
| `Networking/Endpoints/VoiceEndpoints.swift` | voice | `liveToken` `POST /api/live/token {voice, prefer?}`; `transcribe` `POST /api/transcribe {audio, format:"wav", lang}` (`.upload`); `tts` `POST /api/tts {text, lang, gender:"male"}` → raw bytes + content type. | `server-voice.md §3–§5` |
| `Networking/Endpoints/AccountEndpoints.swift` | misc | `announcements` GET (guest or member), `memory` GET/DELETE(`?id=`), `memoryLearn` POST, `version` GET. | `server-misc.md §9`, `server-auth-session-account.md §5.4–5.6` |

Rule: an endpoint helper encodes exactly the documented fields; anything the report marks "ignored"
is not sent. Every helper takes `RequestBudget` from the table above, never the default blindly.
