# Plan — `Features/Shell/` (RootView stub Batch 0; everything else Batch 2)

Interfaces: `INTERFACES.md` → view entry points. Delete `FirasAppShell.swift`, `ShellSidebar.swift`,
`ShellStrings.swift`, `Shell.xcstrings`. Navigation design: `ARCHITECTURE.md §2.8`.

| File | Batch | Purpose | Behaviour | Read |
|---|---|---|---|---|
| `Features/Shell/RootView.swift` | 0 stub / 2 | phase switch | `session.phase`: `.booting` → `MentronXEntryView` (≤ 1.2 s, skippable) then skeleton; `.signedOut` → `ConsentView` (first run) → `LandingView`; `.awaitingVerification` → `AuthView` with the verification card; `.member/.guest` → `AppShell`; `.unreachable` → `AppShell` over cached data + top banner `Strings.Errors.offline` with Retry. Batch 0 stub: `Text("Firas AI")` on `FirasBackground`. | `web-chat-ux.md §1.1–1.6`, `design-brief.md §7.17`, `audit-ios-networking-auth.md §B1 F1` |
| `Features/Shell/AppShell.swift` | 2 | root layout | Fixed-LTR root; `horizontalSizeClass` switch: compact → `ZStack { detail; scrim; CompactDrawer }`; regular → `NavigationSplitView(columnVisibility:)` `.balanced`, sidebar 270–360, `backgroundExtensionEffect()` on 26 inside `#available`. Detail = product screen keyed by `router.product` (`ChatScreen` for `.ai`, `AgentScreen`, `CodeLauncherView`/`CodeWorkspaceView`, `BrainScreen`, `MediaStudioScreen`), each in its own `NavigationStack`. Presents `router.sheet` (`.sheet(item:)`) and `router.cover` (`.fullScreenCover(item:)`); consumes `router.pendingRoute` in `.task`/`.onChange` once. Windows < 320 pt use compact. Hosts `ToastHostView` and `KeyboardCommands`. | `design-brief.md §1, §8`, `audit-ios-shell-settings-design.md §2.1 F6, F10, F19` |
| `Features/Shell/CompactDrawer.swift` | 2 | iPhone drawer | Width `min(360, max(286, w−44))`, `.floating` glass, 30 % scrim, 20 pt edge-swipe `DragGesture` with `@GestureState` offset, momentum projection rule, `FirasMotion.drawerFlick` on release, `FirasMotion.sheet` programmatic; `.accessibilityAddTraits(.isModal)`; `Haptics.select()` on snap. | `design-brief.md §3.1–3.2, §7.2` |
| `Features/Shell/SidebarView.swift` | 2 | sidebar content | Header (lockup, bell + unseen dot, close on compact), `SidebarProductSwitcher`, prominent new-chat row, `SidebarSearch`, `SidebarHistoryList`, usage line (members: `✦ مجاني بالكامل`), guest slot (CTA to sign up), `SidebarAccountPill`. | `design-brief.md §7.2`, `web-chat-ux.md §2, §11, §14`, `web-auth-account-settings.md §4, §5.5` |
| `Features/Shell/SidebarProductSwitcher.swift` | 2 | five rows | `ProductKind.allCases` with symbol + title, selected state, `LiveDot`/count from `jobs.liveCount(product:)`; `⌘1…5`. | `web-agent-ux.md §10`, `design-brief.md §0 (1)` |
| `Features/Shell/SidebarHistoryList.swift` | 2 | history | Pinned shelf then date groups (`اليوم/أمس/هذا الأسبوع/أقدم`), row: title (bidi island), live dot when `jobs.isLive`, swipe pin/delete (7 s undo via `chat.delete`), context menu rename/pin/share/delete, inline rename; five empty states via `EmptyStateView`; `.hoverEffect(.highlight)`. | `web-chat-ux.md §11, Appendix A — Sidebar management`, `design-brief.md §7.2` |
| `Features/Shell/SidebarSearch.swift` | 2 | search field | Title filter; ≥ 3 chars → `chat.search` includes message text; `⌘K` focuses. | `web-chat-ux.md §11` |
| `Features/Shell/SidebarAccountPill.swift` | 2 | account pill | Avatar initial, name (guest: `ضيف`), gear → `router.sheet = .settings(.account)`, sign out. | `web-chat-ux.md §14`, `web-auth-account-settings.md §4` |
| `Features/Shell/KeyboardCommands.swift` | 2 | hardware keys | Hidden button layer with `.keyboardShortcut`: `⌘N` new chat, `⌘K` search, `⌘,` settings, `⌘1…5` products, `⌘⇧M` call, `⌘⇧C` copy last answer, `⌘⇧O` drawer, `esc` stop. `⌘↩` send lives in `ComposerField`. | `design-brief.md §8` |
| `Features/Shell/ToastHostView.swift` | 2 | toast placement | Bottom floating capsule above the composer inset; reads `ToastCenter.current`; action button; auto-dismiss. | `design-brief.md §7.1` |

Rules: no bottom tab bar at the root; screens never present sheets themselves — they set
`router.sheet`; the shell is the only place `layoutDirection` is set to `.leftToRight`.
