# Plan — `Prompting/` (Batch 0, one owner; `PromptCatalog.swift` by another agent)

Interfaces: `INTERFACES.md` → Prompting/. Design: `ARCHITECTURE.md §2.10, §2.12`. Pure functions;
Foundation only; every regex ported from JS is re-checked for `\b` near Arabic (forbidden).

| File | Purpose | Behaviour | Read |
|---|---|---|---|
| `Prompting/PromptCatalog.swift` | (other agent) verbatim web rules | Assume `enum PromptCatalog` with static constants and `systemPrompt(tier:product:mode:lang:think:requestKind:)`; also expected: `planSystem`, `executeNote`, `forcedPlanNote`, `fileGuidance(fmt)`, `searchEmptyNote`, `titlePrompt`, `callSys`, `imageRewritePrompt`, `musicLyricPrompt` — if a constant is missing, `PromptBuilder` owner adds it to `PromptBuilder` as a `static let` with the verbatim text from the report and files a one-line note. | `web-plan-mode.md §2.2`, `server-chat-jobs-chats.md §1.4` |
| `Prompting/RequestClassifier.swift` | intent of the last user message | Ports `_classifyTurn` order: image-edit (picture in play + edit verbs) → image → video → music → longfile (explicit page count + pdf/docx) → longdoc (`LONGDOC_RE`) → file (pdf/docx/pptx/xlsx/csv) → code (`detectCodeRequest`) → irab → chat. `parseExplicitPageCount` accepts Latin and Arabic-Indic digits with "pages"/"صفحة/صفحات". `refersToPreviousImage` = the web's image-follow-up regex. | `web-media-ux.md §1.1–1.4`, `server-code-brainask.md §4.1`, `server-chat-jobs-chats.md §4.1–4.2`, `web-chat-ux.md §8.5` |
| `Prompting/SearchContext.swift` | web search decision + formatting | `trigger`: explicit when toggle on; silent when `benefitsFromSilentSearch` matches and no images; `query` ≤ 280 chars; `run` uses `withDeadline` 8 s (explicit) / 1.5 s (silent), ≤ 6 rows, nonce-fenced block per the web; `wasEmpty` when explicit and zero rows. | `server-chat-jobs-chats.md §1.6`, `web-chat-ux.md §5`, `server-misc.md §1` |
| `Prompting/PromptBuilder.swift` | the single system message + outgoing array | System = catalog + plan addendum by `planTurn` (`.clarifyOrPlan`/`.revision` → base-without-build-rules + planSystem; `.execute` → base-with-build-rules + planSystem + executeNote; `.forcedPlan` adds forcedPlanNote) + `fileGuidance` when kind is file/longfile and turn is not clarify/plan + searchEmptyNote. Then `[user: searchContext]?` + `HistoryWindow` + last user with `fileText` prepended and `images` (or `reattachImages`). `think = thinkToggle && tier.showThinking && !hasImages`; tier → `.pro` when explicit search and tier != max. Never emits the identity block. | `web-plan-mode.md §7.3`, `server-chat-jobs-chats.md §1.2–1.4`, `ARCHITECTURE.md §2.10` |
| `Prompting/MessageSerializer.swift` | wire/persist/merge | `outgoing` strips everything but role/content/images (raw base64, no prefix); `persisted` = whitelist; `merge` by `cid` (server copy wins when its content is longer; local-only rows appended in order; never drops a local user turn). | `server-chat-jobs-chats.md §5.2, §3.6`, `audit-ios-chat.md §Major M2` |
| `Prompting/HistoryWindow.swift` | ≤ 400 000 chars | Walk from the end; always keep the last user turn; system is added by the builder (not counted here); `trimmed` flag for the UI note. | `audit-ios-chat.md §Critical C3` |
| `Prompting/EngineFailureDetector.swift` | busy sentences | The `busyRe` list + empty/whitespace answer → true. | `server-chat-jobs-chats.md §1.8` |
| `Prompting/AutoTitle.swift` | titles | `provisional` = first 42 chars of the first user message (word boundary, `…`); `generate` = `POST /api/chat {nomem:true, tier:"pro", think:false}` with the verbatim title prompt, stream collected, ≤ 60 chars, nil on failure. Caller PUTs `{title}` unless the user renamed. | `server-chat-jobs-chats.md §5.4`, `web-chat-ux.md §11.1` |
| `Prompting/PlanCycle.swift` | state machine | Transitions per §7.2; `userSent` returns the turn kind and mutates phase (snapshot mode on cycle start; `product != .ai` → always `.auto`); `assistantFinished` moves to `awaitingAnswers` (ask parsed) / `awaitingApproval` / `delivered`; `derive(from:snapshot:)` per §7.5; `showsStartPill` = `awaitingApproval`. | `web-plan-mode.md §7.1–7.5, §7.7` |
| `Prompting/AskSpec.swift` | firas-ask parsing + summary | `normalizeAskSpec` rules (§3.2) exactly; accepts ```` ```json ```` or bare JSON with `questions`; `hasOpenAskFence` for the streaming loader; `summary` format §3.5. | `web-plan-mode.md §3.1–3.5, §7.7` |
| `Prompting/ApprovalMatcher.swift` | approval detection | §7.6: normalise via `ArabicText.normalize`, strip trailing `.!؟?…`; exact strings; Arabic prefixes with `(?![\p{L}\p{N}_])`; English whole-message list; > 6 words → false. | `web-plan-mode.md §7.6` |

Rule: nothing here touches stores or UI; all inputs are values so the Chat owner can test by reading.
