# Plan — `DesignSystem/` (Batch 0, one owner)

Interfaces: `INTERFACES.md` → DesignSystem/. Token values: `ARCHITECTURE.md §2.7`. Delete
`GlassSurface.swift`, `FirasCompletionCue.swift`, `Activity.xcstrings`.

| File | Purpose | Behaviour | Read |
|---|---|---|---|
| `DesignSystem/FirasTheme.swift` (keep, extend) | six palettes + derived tokens | Keep the 16 base hexes exactly; add every derived token from the §2.7 table (`userFill` hard-coded hex, `glassWash` colour + `washBlendsLighter`, `glassShadowOpacity`, `grainOpacity`, `callGround`, `codeWarn/codeOk`, `tagColors`). `title` as `LText` (`نهاري/ليلي/أسود/نيلي/كربوني/عنبري`). Remove `AppLanguage`, `FontScale`… no: keep `FontScale`, `ContentWidth`, `MotionPreference` here; `AppLanguage`, `CallVoice`, `DictationDialect` move to Models. `Color(hex:)` stays. | `design-brief.md §6`, `audit-ios-shell-settings-design.md §2 F34` |
| `DesignSystem/PreferencesStore.swift` | the preferences/theme store | Split out of `FirasTheme.swift`; UserDefaults keys unchanged (`theme, lang, tier, fontSize, width, webSearch, thinking, motion, enterSend, imageSharpening, callVoice, dictationLanguage`) + new `responseMode`, `bargeIn`, `uiSounds`, `guestActive`, `consentAccepted`, `notificationsExplained`, `lastSeenAnnouncementAt`. Defaults: dark, `AppLanguage.deviceDefault`, pro, auto, medium, normal, full, search off, thinking off, enter-send off, cedar, `auto` dialect, sounds off. | `design-brief.md §0 (6)`, `audit-ios-shell-settings-design.md §2 F11, F12` |
| `DesignSystem/FirasGlass.swift` | the three glass levels | `firasGlass(.floating)`: `if #available(iOS 26, *)` → `.glassEffect(Glass.clear.tint(palette.glassTint).interactive(), in: shape)` + overlay `shape.fill(palette.glassWash)` with `.blendMode(.plusLighter)` when `washBlendsLighter` + `shape.stroke(palette.glassStroke, lineWidth: 0.5)`; else `.background(shape.fill(palette.surface.opacity(0.28)))` + `.background(.ultraThinMaterial.opacity(0.62), in: shape)` + wash + 1 pt stroke; shadow `.black.opacity(palette.glassShadowOpacity) r24 y8`. `.sheet`: 26 `Glass.regular.tint`; 18 material + `surface.opacity(0.55)`. `.chrome`: no-op on 26; `.toolbarBackground(.ultraThinMaterial, for: .navigationBar)` on 18. `@Environment(\.accessibilityReduceTransparency)` → solid `surface` + stroke. `Glass.clear` spelled in exactly one private function. `bidiIsland`, `forceLTR`, `readingColumn`, `firasSheetBackground`. | `design-brief.md §2.1–2.5`, `audit-ios-shell-settings-design.md §2.1 F5` |
| `DesignSystem/FirasBackground.swift` | ground | Move from `GlassSurface.swift`: ground + accent radial (0.16; 0.08 on black) + bottom darkening + optional static grain (128 px tile, `grainOpacity`, never animated) + welcome halo (static 55 % under reduced motion). | `design-brief.md §2.4 (7), §7.1` |
| `DesignSystem/SurfaceCard.swift` | the only card container | Opaque `surface`, hairline `border`, radius 9 (nested 7), shadow `.black.opacity(0.05) r3 y1`. | `design-brief.md §2.3, §2.5` |
| `DesignSystem/Typography.swift` | `FirasType`, `firasTracking` | `.body` scaled by `FontScale.factor` via `.font(.system(size: 17 * factor))`; line spacing 9/6; mono `.system(.body, design: .monospaced)`; tracking only without Arabic scalars; Arabic never `.light`. | `design-brief.md §4.1` |
| `DesignSystem/FirasMotion.swift` | springs + gate | Values from §2.7; `isOn` = `prefs.motionEnabled && !reduceMotion`; `fade` 120 ms. | `design-brief.md §3` |
| `DesignSystem/Haptics.swift` | vocabulary | Prepared generators (`prepare()` in `prepareCompletion`); guard `UIApplication.shared.applicationState == .active`; coalesce `attach` within 120 ms. | `design-brief.md §5.1` |
| `DesignSystem/FirasSound.swift` | two UI sounds | Preloaded `AVAudioPlayer`s for `send.caf`/`done.caf` **if present in the bundle** (missing → silent no-op); `.ambient` via `AudioSessionArbiter.acquire(.uiSound)`; volume 0.6; skipped when `!prefs.uiSoundsEnabled || callActive`. | `design-brief.md §5.2` |
| `DesignSystem/Components.swift` | `FirasPill`, `FirasIconButton`, `LiveDot`, `SkeletonView`, `EmptyStateView` | 44 pt hit targets; `LiveDot` pulses (opacity only under reduced motion); skeleton shimmer becomes static blocks when motion is off. | `design-brief.md §7.2 (empty states), §3.3` |
| `DesignSystem/ToastCenter.swift` | toast queue + host view `ToastHostView` | One at a time, 3.2 s (7 s with action), `.floating` capsule above the composer, error tint, VoiceOver announcement, `performAction` runs the undo. | `design-brief.md §7.1`, `web-chat-ux.md §11 (undo)` |
| `DesignSystem/FirasActivityLabel.swift` (keep, fix) | busy label | Replace the 30 fps `TimelineView` with `phaseAnimator`/`repeatForever` sweep; reduced motion → 1.5 s opacity pulse 0.32↔1 (never static). | `audit-ios-shell-settings-design.md §2 F29`, `design-brief.md §3.3` |
| `DesignSystem/FirasBrandMark.swift` (keep) | mark + wordmark | unchanged | — |
| `DesignSystem/MentronXEntryView.swift` (keep, shorten) | intro | ≤ 1.2 s, tap to skip, first run + cold auth only. | `audit-ios-shell-settings-design.md §2 F28` |

Rules: no view outside this folder spells `glassEffect`, `Glass`, `GlassEffectContainer` (the
composer and call controls may use `GlassEffectContainer` inside their own `#available` block — those
two files only). Nobody writes `.opacity(0.0x)` inline for chrome; use palette tokens.
